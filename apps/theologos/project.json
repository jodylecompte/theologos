{
  "name": "theologos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "app",
  "sourceRoot": "apps/theologos/src",
  "tags": ["scope:theologos"],
  "targets": {
    "build": {
      "executor": "@angular/build:application",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/theologos",
        "browser": "apps/theologos/src/main.ts",
        "polyfills": ["zone.js"],
        "tsConfig": "apps/theologos/tsconfig.app.json",
        "assets": [
          {
            "glob": "**/*",
            "input": "apps/theologos/public"
          }
        ],
        "styles": ["apps/theologos/src/styles.css"],
        "server": "apps/theologos/src/main.server.ts",
        "ssr": {
          "entry": "apps/theologos/src/server.ts"
        },
        "outputMode": "server"
      },
      "configurations": {
        "production": {
          "budgets": [
            {
              "type": "initial",
              "maximumWarning": "500kb",
              "maximumError": "1mb"
            },
            {
              "type": "anyComponentStyle",
              "maximumWarning": "4kb",
              "maximumError": "8kb"
            }
          ],
          "outputHashing": "all"
        },
        "development": {
          "optimization": false,
          "extractLicenses": false,
          "sourceMap": true
        }
      },
      "defaultConfiguration": "production"
    },
    "serve": {
      "dependsOn": ["api:serve"],
      "continuous": true,
      "executor": "@angular/build:dev-server",
      "configurations": {
        "production": {
          "buildTarget": "theologos:build:production"
        },
        "development": {
          "buildTarget": "theologos:build:development"
        }
      },
      "defaultConfiguration": "development",
      "options": {
        "proxyConfig": "apps/theologos/proxy.conf.json"
      }
    },
    "extract-i18n": {
      "executor": "@angular/build:extract-i18n",
      "options": {
        "buildTarget": "theologos:build"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/apps/theologos"
      }
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "theologos:build",
        "port": 4200,
        "staticFilePath": "dist/apps/theologos/browser",
        "spa": true
      }
    }
  }
}
